[project]
name = "ragxiv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "langchain>=1.4.2",
    "langchain-core>=1.6.4",
    "lark>=1.3.1",
    "openai>=3.19.2",
    "pgvector>=0.5.0",
    "pymupdf>=1.28.2",
    "pytest>=9.1.1",
    "pyyaml>=6.0.3",
]

[tool.pytest.ini_options]
markers = [
    "integration: tests that make real external service calls",
]
